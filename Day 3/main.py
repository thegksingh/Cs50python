#strip remove extract spaces user add by mistake between respons 
#input and title capitalize every first alphabet in each words 
#we can also use .capitalize() but is only capitalized first alphabet of first word

name = input ("what's your name? ").strip().title()
print (f"Hello, {name}")
 